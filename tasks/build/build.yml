---
platform: linux

image_resource:
  type: docker-image
  source: { repository: maven, tag: 3.5.0-jdk-8-alpine }

inputs:
  - name: pipeline-src
  - name: api-src
  - name: api-version

outputs:
  - name: build

caches:
  - path: .m2

run:
  path: pipeline-src/api/tasks/build.sh
